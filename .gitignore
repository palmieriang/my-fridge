# ---------------------------------------------------- #
# General
# ---------------------------------------------------- #

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
node_modules/

# Output of 'npm pack'
*.tgz

# Yarn Integrity
.yarn-integrity

# Optional cache directories
.npm
.pnp.*
.cache/

# TypeScript
*.tsbuildinfo

# ESLint
.eslintcache

# REPL & Shell
.node_repl_history
*.swp
*.swo
*~

# OS generated
.DS_Store

# JetBrains IDEs
.idea/

# VS Code
.vscode/

# Coverage
coverage/
*.lcov
.nyc_output

# ---------------------------------------------------- #
# Environment Files
# ---------------------------------------------------- #

.env
.env.*
!.env.example

# ---------------------------------------------------- #
# Expo & Metro
# ---------------------------------------------------- #

.expo/
!.expo/settings.json # settings.json can be useful to commit if it has shared config

# Expo asset registry
# If your assets/index.js is auto-generated and should NOT be committed, keep this line.
# If you manage assets manually and this file defines them, you might want to commit it.
# Usually, actual asset files (images, fonts) are committed, not their index files.
# assets/index.js

# Metro cache
/metro-cache/

# ---------------------------------------------------- #
# Native Project Folders (Generated by Expo Prebuild / EAS Build)
# If you are using Expo's Prebuild / Managed Workflow for native projects,
# these folders should typically NOT be committed to Git.
# They will be regenerated based on your app.json/app.config.js and EAS configurations.
# ---------------------------------------------------- #

/android
/ios

# The lines below are now largely redundant if /android and /ios are ignored,
# but keeping them commented out for reference if you ever switch back to a bare workflow.
# ---------------------------------------------------- #
# Android (Prebuild / Native) - Redundant if /android is ignored
# ---------------------------------------------------- #

# /android/.gradle/
# /android/build/
# /android/app/build/
# /android/app/.cxx/
# /android/local.properties # Contains local SDK path, should not be committed
# /android/*.iml            # IDE module files
# /android/.idea/           # IDE project files

# IMPORTANT: These files are essential for reproducible builds and MUST be committed.
# If you ignore the whole /android folder, these files are also ignored.
# You would only need to uncomment these if you *are* committing the /android folder.
# /android/gradlew
# /android/gradlew.bat
# /android/gradle/wrapper/gradle-wrapper.*


# ---------------------------------------------------- #
# iOS (Prebuild / Native) - Redundant if /ios is ignored
# ---------------------------------------------------- #

# /ios/build/
# /ios/Pods/
# /ios/.symlinks/
# /ios/DerivedData/
# /ios/*.xcworkspace/          # Can be ignored if generated by Pods and not manually maintained
# /ios/*.pbxuser               # User-specific Xcode settings
# /ios/*.mode1v3               # User-specific Xcode settings
# /ios/*.mode2v3               # User-specific Xcode settings
# /ios/*.perspectivev3         # User-specific Xcode settings
# /ios/*.xcuserstate          # User-specific Xcode settings
# /ios/.swiftpm/               # Swift Package Manager cache
# /ios/.xcode.env.local        # Local Xcode environment variables
# /ios/xcuserdata/           # Xcode user-specific data
# /ios/.idea/                  # IDE project files

# IMPORTANT: This file is essential for your iOS project structure and MUST be committed.
# If you ignore the whole /ios folder, this file is also ignored.
# You would only need to uncomment this if you *are* committing the /ios folder.
# /ios/*.xcodeproj/