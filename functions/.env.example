# Environment Configuration for Firebase Functions

# ============================================================================
# DEVELOPMENT ENVIRONMENT (Local/Emulator)
# ============================================================================
# In development (NODE_ENV=development), all features are auto-enabled
# for testing purposes. This is safe because emulators have no cost.

NODE_ENV=development

# ============================================================================
# PRODUCTION ENVIRONMENT
# ============================================================================
# In production (NODE_ENV=production), features must be explicitly enabled
# Copy this file to .env.production and uncomment the features you want:

# NODE_ENV=production
# PROD_ENABLE_NOTIFICATIONS=false
# PROD_ENABLE_SERVER_VALIDATION=false

# COST IMPLICATIONS (Production Only):
# PROD_ENABLE_NOTIFICATIONS=true: ~$0.40/month per 1000 active users
# PROD_ENABLE_SERVER_VALIDATION=true: ~$0.10/month per 1000 products created

# DEPLOYMENT:
# firebase deploy --only functions
